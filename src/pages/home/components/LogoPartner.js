import React from "react";
import { styled } from "@mui/system";

export default function LogoPartner() {
  return <Wrapper>LogoPartner</Wrapper>;
}
const Wrapper = styled("div")({
  width: "100%",
  position: "absolute",
  bottom: 0,
});
